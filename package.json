{
  "name": "liquid-staking-governance",
  "version": "1.0.0",
  "description": "Multi-chain liquid staking protocol with off-chain voting and cross-chain execution",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "demo": "hardhat run scripts/demo.ts --network localhost",
    "snapshot": "hardhat run scripts/snapshot-builder.ts --network localhost",
    "signatures": "hardhat run scripts/signature-collector.ts --network localhost",
    "relay": "hardhat run scripts/relay.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.19.0",
    "hardhat-gas-reporter": "^1.0.9",
    "solidity-coverage": "^0.8.5",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "dependencies": {
    "ethers": "^6.8.0",
    "merkletreejs": "^0.3.9",
    "keccak": "^3.0.4"
  }
}
